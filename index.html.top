<!DOCTYPE html>
<html lang="pt-pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes, viewport-fit=cover">
    <title>Frequência Mental Poderosa - Despertar do Rugido | Imagens Perfeitas</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500;1,600&display=swap" rel="stylesheet">
    <style>
        /* RESET + base sem scroll lateral */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        html, body {
            overflow-x: hidden;
            width: 100%;
            position: relative;
        }
        body {
            font-family: 'Inter', sans-serif;
            background-color: #D6CFA1;
            color: #1F1F1F;
            scroll-behavior: smooth;
            overflow-x: hidden;
        }
        .serif { font-family: 'Playfair Display', serif; }
        section { padding-top: 5rem; padding-bottom: 5rem; }
        @media (min-width: 768px) {
            section { padding-top: 8rem; padding-bottom: 8rem; }
        }
        .text-humanized { line-height: 1.8; letter-spacing: -0.01em; }

        /* IMAGE BOXES: agora as imagens cobrem 100% da área sem espaços */
        .image-box {
            overflow: hidden;
            border-radius: 2rem;
            box-shadow: 0 30px 50px rgba(0, 0, 0, 0.1);
            background-color: #1a1a1a; /* fundo neutro para transição, mas a imagem cobre tudo */
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 0; /* sem padding interno para não criar bordas */
            transition: all 0.2s ease;
            transform: translateZ(0);
        }
        .image-box img {
            width: 100%;
            height: 100%;
            object-fit: cover; /* IMPERATIVO: preenche toda a caixa, cortando o excedente mas sem distorcer */
            display: block;
            transition: transform 1.2s cubic-bezier(0.2, 0.9, 0.4, 1.1);
            backface-visibility: hidden;
        }
        /* Hover apenas desktop */
        @media (hover: hover) and (pointer: fine) {
            .image-box:hover img { transform: scale(1.03); }
        }
        /* Mobile: altura mais flexível, sem hover */
        @media (max-width: 768px) {
            .image-box { min-height: 280px; max-height: 440px; }
            .image-box:hover img { transform: none; }
        }

        .glass-tag {
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(8px);
            border: 1px solid rgba(255, 255, 255, 0.25);
        }
        .step-number {
            font-size: 5rem;
            opacity: 0.12;
            font-family: 'Playfair Display', serif;
            position: absolute;
            top: -1.8rem;
            left: -0.8rem;
            font-weight: 600;
            pointer-events: none;
        }

        /* ========== ANIMAÇÕES E MOVIMENTOS ========== */
        .fade-up {
            opacity: 0;
            transform: translateY(40px);
            transition: opacity 0.9s cubic-bezier(0.2, 0.9, 0.4, 1.1), transform 0.9s cubic-bezier(0.2, 0.9, 0.4, 1.1);
        }
        .fade-up.visible {
            opacity: 1;
            transform: translateY(0);
        }
        .horizontal-move {
            transition: transform 0.4s ease-out;
            will-change: transform;
        }
        .parallax-img {
            transition: transform 0.1s linear;
            will-change: transform;
        }
        .floating-element {
            animation: floatHorizontal 6s ease-in-out infinite;
        }
        @keyframes floatHorizontal {
            0% { transform: translateX(0px) translateY(0px); }
            50% { transform: translateX(12px) translateY(-8px); }
            100% { transform: translateX(0px) translateY(0px); }
        }
        .stagger-card {
            transition: transform 0.5s cubic-bezier(0.2, 0.8, 0.4, 1);
        }
        .no-x-overflow {
            overflow-x: hidden;
        }
    </style>
</head>
<body class="antialiased no-x-overflow">

    <!-- 1. HERO SECTION -->
    <header class="min-h-screen flex items-center px-5 md:px-6 py-12 relative overflow-hidden">
        <div class="absolute top-20 right-5 w-40 h-40 bg-white/10 rounded-full blur-3xl floating-element pointer-events-none"></div>
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-12 md:gap-20 items-center">
            <div class="space-y-7 fade-up">
                <div class="text-2xl md:text-3xl font-bold tracking-tighter serif italic opacity-90">Frequência.</div>
                <h1 class="text-5xl sm:text-6xl md:text-8xl font-light serif leading-[1.1] tracking-tight">
                    O silêncio que precede o seu <span class="italic font-normal">rugido</span>.
                </h1>
                <p class="text-lg md:text-2xl text-humanized opacity-85 max-w-lg">
                    A desmotivação não é ausência de força; é uma frequência desalinhada. Recupere a sua soberania mental hoje.
                </p>
                <div class="pt-4">
                    <a href="#golem" class="inline-block bg-black text-white px-10 md:px-12 py-5 rounded-full text-sm font-bold uppercase tracking-widest hover:scale-105 transition-all shadow-xl">
                        Identificar meu bloqueio
                    </a>
                </div>
            </div>
            <div class="image-box h-[420px] sm:h-[480px] md:h-[600px] w-full fade-up" style="transition-delay: 0.15s;">
                <img src="./hero-leao.jpg" alt="Leão majestoso" class="parallax-img">
            </div>
        </div>
    </header>

    <!-- 2. EFEITO GOLEM -->
    <section id="golem" class="px-5 md:px-6 bg-[#2D2D2D] text-[#D6CFA1]" data-scroll>
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-12 md:gap-20 items-center">
            <div class="image-box h-[380px] sm:h-[450px] md:h-[550px] w-full md:order-1 order-2 fade-up">
                <img src="./ebook-golem.png" alt="E-book Efeito Golem" class="parallax-img">
            </div>
            <div class="space-y-7 md:order-2 order-1 fade-up">
                <span class="glass-tag px-5 py-1.5 text-[10px] font-bold tracking-[0.3em] uppercase rounded-full inline-block">Recurso Gratuito</span>
                <h2 class="text-4xl md:text-7xl font-light serif leading-tight">O Peso Invisível: <br><span class="italic">Efeito Golem</span>.</h2>
                <p class="text-base md:text-xl text-humanized opacity-85">
                    Descubra como as expectativas alheias esculpiram a sua estagnação. Este guia é o primeiro passo para quebrar o feitiço da mediocridade.
                </p>
                <a href="./glm.pdf" download class="inline-flex items-center gap-4 px-8 py-5 bg-[#D6CFA1] text-black rounded-xl font-bold uppercase tracking-widest text-sm hover:bg-white transition-all shadow-2xl w-fit">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path></svg>
                    Baixar Guia Gratuito (PDF)
                </a>
            </div>
        </div>
    </section>

    <!-- 3. ORIGEM DO CONFLITO -->
    <section class="px-5 md:px-6 bg-[#E5E0C2]">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-12 md:gap-20 items-center">
            <div class="space-y-7 fade-up">
                <h2 class="text-4xl md:text-6xl font-light serif leading-tight">Porquê se sente <br><span class="italic underline decoration-2">estagnado?</span></h2>
                <p class="text-lg md:text-xl text-humanized opacity-80">
                    Passamos anos a absorver ruído. O ruído do medo, da dúvida e das comparações. Esse ruído cria uma névoa que impede o leão de ver a savana. 
                </p>
                <p class="text-base md:text-lg text-humanized opacity-75">
                    Não se trata de falta de ferramentas. Trata-se de uma bateria interna que está a descarregar em ciclos repetitivos de pensamento negativo.
                </p>
            </div>
            <div class="image-box h-[380px] sm:h-[450px] md:h-[520px] w-full fade-up">
                <img src="./origem-conflito.jpg" alt="Conflito mental" class="parallax-img">
            </div>
        </div>
    </section>

    <!-- 4. FILOSOFIA E CONSCIÊNCIA -->
    <section id="filosofia" class="px-5 md:px-6" data-scroll>
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-12 md:gap-20 items-center">
            <div class="image-box h-[420px] sm:h-[480px] md:h-[600px] w-full md:order-1 order-2 fade-up">
                <img src="./meditacao.png" alt="Meditação consciente" class="parallax-img">
            </div>
            <div class="space-y-7 md:order-2 order-1 fade-up">
                <span class="text-xs uppercase tracking-[0.3em] opacity-60 block font-semibold">Consciência</span>
                <h2 class="text-4xl md:text-6xl font-light serif leading-snug">Você não nasceu para apenas <span class="italic underline decoration-1">suportar</span> o dia.</h2>
                <p class="text-xl md:text-2xl text-humanized text-gray-800 italic font-medium">
                    "O desânimo é o grito de uma alma que possui um potencial maior do que a rotina que a aprisiona."
                </p>
                <p class="text-base md:text-lg text-humanized opacity-80">
                    A meditação e a introspecção não são fugas da realidade, são os treinos necessários para dominar o território da própria mente.
                </p>
            </div>
        </div>
    </section>

    <!-- 5. MÉTODO DOS 3 PILARES -->
    <section class="px-5 md:px-6 bg-[#1F1F1F] text-[#D6CFA1] overflow-hidden">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl md:text-6xl serif italic text-center mb-16 md:mb-24 fade-up">A Trindade do Despertar</h2>
            <div class="grid md:grid-cols-3 gap-12 md:gap-16">
                <div class="relative p-8 md:p-10 border border-[#D6CFA1]/20 rounded-3xl bg-[#2a2a2a]/40 horizontal-move stagger-card fade-up" data-horizontal="-12px" style="transition-delay: 0.1s;">
                    <span class="step-number">01</span>
                    <h3 class="text-2xl md:text-3xl serif mb-5">Purificação</h3>
                    <p class="opacity-80 leading-relaxed">Elimine os parasitas mentais e o Efeito Golem que têm drenado a sua energia vital durante anos.</p>
                </div>
                <div class="relative p-8 md:p-10 border border-[#D6CFA1]/20 rounded-3xl bg-[#2a2a2a]/40 horizontal-move stagger-card fade-up" data-horizontal="0px" style="transition-delay: 0.2s;">
                    <span class="step-number">02</span>
                    <h3 class="text-2xl md:text-3xl serif mb-5">Sintonização</h3>
                    <p class="opacity-80 leading-relaxed">Ajuste a sua frequência vibracional para o modo "Criador" em vez do modo "Espectador".</p>
                </div>
                <div class="relative p-8 md:p-10 border border-[#D6CFA1]/20 rounded-3xl bg-[#2a2a2a]/40 horizontal-move stagger-card fade-up" data-horizontal="12px" style="transition-delay: 0.3s;">
                    <span class="step-number">03</span>
                    <h3 class="text-2xl md:text-3xl serif mb-5">Manifestação</h3>
                    <p class="opacity-80 leading-relaxed">Transforme a clareza mental em resultados tangíveis no mundo físico, com a força de um rugido.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- 6. RUGIDO INTERNO -->
    <section class="px-5 md:px-6">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-12 md:gap-20 items-center">
            <div class="space-y-7 fade-up">
                <h2 class="text-4xl md:text-6xl font-light serif leading-tight">O Momento da <br><span class="italic font-normal">Transmutação</span>.</h2>
                <p class="text-lg md:text-xl text-humanized opacity-85">
                    Quando a mente se alinha com o propósito, o medo desaparece. Não porque os problemas sumiram, mas porque você se tornou maior do que eles.
                </p>
                <p class="text-base md:text-lg text-humanized opacity-75">
                    Este não é apenas um curso. É o resgate de uma autoridade que você sempre possuiu, mas que lhe ensinaram a esquecer.
                </p>
            </div>
            <div class="image-box h-[380px] sm:h-[450px] md:h-[520px] w-full fade-up">
                <img src="./foco-absoluto.png" alt="Foco e poder" class="parallax-img">
            </div>
        </div>
    </section>

    <!-- 7. CHECKOUT FINAL -->
    <section id="obra" class="px-5 md:px-6 bg-black text-[#D6CFA1]" data-scroll>
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-12 md:gap-20 items-center">
            <div class="relative md:order-1 order-2 flex justify-center fade-up">
                <div class="image-box h-[420px] sm:h-[520px] md:h-[650px] w-full transform -rotate-1 shadow-2xl floating-element" style="animation-duration: 8s;">
                    <img src="./mockup-principal.png" alt="E-book Frequência Mental Poderosa">
                </div>
                <div class="absolute -bottom-8 -right-4 md:-bottom-10 md:-right-6 bg-[#D6CFA1] text-black w-32 h-32 md:w-36 md:h-36 rounded-full flex flex-col items-center justify-center shadow-2xl border-4 md:border-8 border-black z-10">
                    <span class="text-3xl md:text-4xl font-bold serif leading-none">7</span>
                    <span class="text-[8px] md:text-[9px] uppercase font-bold tracking-widest text-center mt-1">Dias de <br> Garantia</span>
                </div>
            </div>
            <div id="comprar" class="space-y-8 md:space-y-10 md:order-2 order-1 fade-up">
                <h2 class="text-4xl md:text-7xl font-light serif leading-tight">Frequência Mental Poderosa.</h2>
                <h3 class="text-xl md:text-2xl serif italic opacity-80 -mt-4">O Despertar do Rugido Interno</h3>
                <p class="text-base md:text-xl text-humanized opacity-80">
                    O mapa definitivo para a sua transmutação pessoal. Saia da apatia e desperte a autoridade natural que sempre esteve aí.
                </p>
                <div class="space-y-6">
                    <button class="w-full py-6 md:py-8 bg-[#D6CFA1] text-black rounded-full font-bold text-lg md:text-xl uppercase tracking-widest hover:scale-105 transition-all shadow-2xl">
                        Despertar Meu Potencial
                    </button>
                    <p class="text-center text-[9px] md:text-[10px] opacity-50 uppercase tracking-[0.3em]">
                        Risco Zero • Acesso Vitalício • Purificação Mental
                    </p>
                </div>
            </div>
        </div>
    </section>

    <footer class="py-16 md:py-20 px-6 text-center opacity-40">
        <div class="max-w-7xl mx-auto text-[10px] md:text-xs uppercase tracking-[0.4em]">
            Frequência © 2026 • O despertar é uma escolha.
        </div>
    </footer>

    <script>
        (function() {
            // 1. Prevenir scroll horizontal
            function fixHorizontalOverflow() {
                document.body.style.overflowX = 'hidden';
                document.documentElement.style.overflowX = 'hidden';
            }
            window.addEventListener('resize', fixHorizontalOverflow);
            fixHorizontalOverflow();

            // 2. Intersection Observer para fade-up
            const fadeElements = document.querySelectorAll('.fade-up');
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('visible');
                        observer.unobserve(entry.target);
                    }
                });
            }, { threshold: 0.15, rootMargin: "0px 0px -20px 0px" });
            fadeElements.forEach(el => observer.observe(el));

            // 3. Movimento horizontal nos cards (scroll)
            const horizontalCards = document.querySelectorAll('.horizontal-move');
            if (horizontalCards.length) {
                const handleScrollHorizontal = () => {
                    const scrollY = window.scrollY;
                    horizontalCards.forEach((card, idx) => {
                        const rect = card.getBoundingClientRect();
                        const viewportHeight = window.innerHeight;
                        const isVisible = rect.top < viewportHeight - 100 && rect.bottom > 100;
                        if (isVisible) {
                            const progress = Math.min(1, Math.max(0, (viewportHeight - rect.top) / (viewportHeight + rect.height)));
                            let moveX = 0;
                            if (card.dataset.horizontal) {
                                moveX = parseFloat(card.dataset.horizontal) * progress;
                            } else {
                                const sign = idx === 0 ? -1 : (idx === 2 ? 1 : 0);
                                moveX = sign * 12 * progress;
                            }
                            card.style.transform = `translateX(${moveX}px)`;
                        } else {
                            card.style.transform = `translateX(0px)`;
                        }
                    });
                };
                window.addEventListener('scroll', handleScrollHorizontal);
                window.addEventListener('resize', handleScrollHorizontal);
                handleScrollHorizontal();
            }

            // 4. Parallax vertical suave nas imagens
            const parallaxImages = document.querySelectorAll('.parallax-img');
            if (parallaxImages.length) {
                window.addEventListener('scroll', () => {
                    parallaxImages.forEach(img => {
                        const parentBox = img.closest('.image-box');
                        if (parentBox) {
                            const rect = parentBox.getBoundingClientRect();
                            const viewportCenter = window.innerHeight / 2;
                            const elementCenter = rect.top + rect.height / 2;
                            const distanceFromCenter = (elementCenter - viewportCenter) / 35;
                            const moveY = Math.min(20, Math.max(-20, distanceFromCenter * 0.6));
                            img.style.transform = `translateY(${moveY}px)`;
                        }
                    });
                });
            }

            // 5. Âncoras suaves
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function(e) {
                    const targetId = this.getAttribute('href');
                    if (targetId === "#" || targetId === "") return;
                    const targetElement = document.querySelector(targetId);
                    if (targetElement) {
                        e.preventDefault();
                        const offsetTop = targetElement.getBoundingClientRect().top + window.scrollY - 20;
                        window.scrollTo({ top: offsetTop, behavior: 'smooth' });
                    }
                });
            });

            // 6. Mobile: reduzir intensidade do parallax para melhor performance
            if ('ontouchstart' in window) {
                const mobileParallax = document.querySelectorAll('.parallax-img');
                mobileParallax.forEach(img => {
                    img.style.transform = 'translateY(0px)';
                });
                const cardsMobile = document.querySelectorAll('.horizontal-move');
                cardsMobile.forEach(card => {
                    card.style.transition = 'transform 0.2s ease-out';
                });
            }
        })();
    </script>
</body>
</html>
